/*
use crate::errors::Error;
use crate::fs::fat::Fat12;
use crate::println;
use alloc::vec::Vec;

pub mod fat;

/// Initializes the file system.
///
/// # Returns
///
/// * `Result<Vec<Fat12>, Error>` - The FAT file systems.
///
/// # Errors
///
/// * If the FAT file system fails to initialize.
pub fn init() -> Result<Vec<Fat12>, Error> {
    println!("[INFO]: Initializing the FAT file system...");

    fat::init()
}
 */
